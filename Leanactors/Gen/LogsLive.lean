-- GENERATED by elixir/to_lean.exs. Do not edit.
-- Not translated (public API, not a callback): LogsLive.mount/3, LogsLive.render/1
-- Not translated (a LiveView browser event is not a message, but it IS a transition of the real process, which this model does not carry): LogsLive.handle_event/3
import Leanactors.Sys
import Leanactors.Term

namespace Leanactors.Gen.LogsLive

open Leanactors


inductive Msg
  | new_log_entry (a0 : Term)
  deriving Repr, DecidableEq

inductive St
  | logs_live (entries : List Term)
  deriving Repr, DecidableEq


/-- Who traps exits (from `Process.flag(:trap_exit, true)`), the EXIT message, the DOWN message. -/
def sig : Signals St Msg where
  traps := fun
    | .logs_live _ => false
  -- no module declares {:EXIT, ...}; nobody traps, so this codec is never used
  exitMsg := fun _ _ => .new_log_entry (Term.mk 0)

def beh : EBehavior St Msg
  | _, _, .logs_live entries, .new_log_entry entry => (.logs_live (entries ++ [entry]), [])

end Leanactors.Gen.LogsLive

