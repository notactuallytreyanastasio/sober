-- GENERATED by elixir/to_lean.exs. Do not edit.
-- Not translated (public API, not a callback): RadioLive.mount/3, RadioLive.render/1
-- Not translated (a LiveView browser event is not a message, but it IS a transition of the real process, which this model does not carry): RadioLive.handle_event/3
import Leanactors.Sys
import Leanactors.Term

namespace Leanactors.Gen.RadioLive

open Leanactors


inductive Msg
  | now_playing (a0 : Option Term)
  deriving Repr, DecidableEq

inductive St
  | radio_live (now_playing : Option Term) (playing : Bool)
  deriving Repr, DecidableEq


/-- Who traps exits (from `Process.flag(:trap_exit, true)`), the EXIT message, the DOWN message. -/
def sig : Signals St Msg where
  traps := fun
    | .radio_live _ _ => false
  -- no module declares {:EXIT, ...}; nobody traps, so this codec is never used
  exitMsg := fun _ _ => .now_playing none

def beh : EBehavior St Msg
  | _, _, .radio_live _ _, .now_playing track_info => (.radio_live track_info (track_info ≠ none), [])

end Leanactors.Gen.RadioLive

