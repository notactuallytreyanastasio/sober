{
  "author": "Bobby Grayson",
  "change_id": "48eb7a5a-3593-4a8f-ad1a-b627423da256",
  "created_at": "2026-09-10T21:39:48.005349-04:00",
  "description": "FILES TOUCHED: new Leanactors/Term.lean, Leanactors/Gen/TableRegistry.lean, Leanactors/Examples/TableRegistry.lean, elixir/real/table_registry.ex, elixir/table_registry.exs, elixir/test/fixtures/{untyped,record_state,ets_resource,error_try_rescue}.ex and their expected/*.lean; modified Leanactors/AssocList.lean (uniqueness group appended), Leanactors.lean (two imports: Term after AssocList, Examples.TableRegistry last), check.sh (one translate line + one diff line + one driver line), elixir/to_lean.exs.\nREADME Status: EIGHT worked examples (add table_registry); 41 fixtures (30 ok, 9 error); eight Gen files. The table registry has both its properties proved AND checked. Table row: | Table registry | a team maps to at most one ETS table, and distinct teams never share one | 11,737, mutation-tested (no counter bump) |\nREADME Layout rows to add: | Leanactors/Term.lean | An opaque term, a structure over Nat with DecidableEq and Repr: the type of every field whose Elixir type the translator does not know (untyped mode, term()/any()/reference(), the reference :ets.new returns), with Term.fresh and the id lemmas | ; | Leanactors/Examples/TableRegistry.lean | Loom.Teams.TableRegistry translated with no annotations at all: hand beh, beh_eq_gen, bounded check of uniqueness, the no-counter-bump mutant, and the Bounded/Uniq invariant proof of team_has_one_ref and refs_unique | ; | elixir/real/*.ex | Real modules copied verbatim from other projects, translated as they stand | ; | elixir/table_registry.exs | Driver: real ETS tables, one per team, re-create gives a fresh reference, delete is idempotent, the rescue keeps the registry alive |\nREADME AssocList row: add get?/insert/erase ... plus the uniqueness group Uniq / key_inj / val_inj / insert_fresh / Uniq.erase and mem_erase_of_ne.\nREADME new paragraph **Untyped mode** (text): A module that declares no @type has its declarations inferred from its own source before anything else runs, so every decision downstream is still type-directed. Message unions come from the handle_cast/handle_info/handle_call clause patterns (a tag at the arity it is matched with, the callback fixing the kind) and from the literal messages the module sends that nobody handles; the reply type from every {:reply, r, _} in the file when all are literal atoms or tagged tuples (a tag at two arities, :ok and {:ok, ref}, names the tuple form ok1); the state from the literal of init/1, where a map with atom keys is a record. Every inferred field is term(), rendered as the opaque Term of Leanactors/Term.lean, which carries a number and nothing else: enough for DecidableEq, which every map key needs, and for fresh references. A @type declaration, where there is one, refines those fields exactly as before, which is what gradual typing means here; the seven annotated sources translate byte for byte.\nREADME new paragraph **Record-shaped state**: a @type state or an init literal %{k: e, ..} with atom keys becomes one St constructor with NAMED fields. A whole-state variable binds the fields the body reads (state.f is the field, %{state | f: e} rebuilds the constructor with that field replaced, a bare state is the whole constructor) and the others are _; a map pattern %{f: p, ..} = state binds the named fields to their sub-patterns. Those are real Lean patterns, unlike an association-list map pattern, which is a variable plus inlined guards, so the clause-subsumption test had to learn the difference.\nREADME new paragraph **External resources**: :ets.new(..) on the right of a binding is a fresh opaque reference. The state gets a hidden trailing counter (ets : Nat) like the after-timer generation, the k-th table a body creates is Term.mk (ets + k), and the continuing state advances the counter. :ets.delete(ref) and the try .. rescue .. end around it are dropped before translation: what happens inside ETS is not modelled at all, so what is proved about the table registry is a property of its map of references, not of ETS. A try with any other body is an error. Public defs that are not callbacks (the module's own API wrappers around GenServer.call, raise included) are not translated; the generated file names them in a comment.\nNOTES round-6 bullet: The first real file. Loom.Teams.TableRegistry went in verbatim, with nothing annotated and nothing deleted, and came out with two proved properties. What it cost: inference of the shapes a @type would have given, one opaque type, named state fields, a counter standing in for the world outside the process, and the decision that an external resource's effects are simply not in the model. What it bought: the proof is about the registry's map, and it is honest about that.",
  "metadata": {
    "branch": "main",
    "confidence": 85
  },
  "node_type": "observation",
  "status": "pending",
  "title": "Integrator record for wf6/untyped: README/NOTES replacement text",
  "updated_at": "2026-09-10T21:39:48.005349-04:00"
}
